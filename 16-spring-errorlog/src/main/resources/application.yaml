server:
  port: 8090

spring:
  application:
    name: hfwas-log-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-test
    username: root

  # Redis 配置。Redisson 默认的配置足够使用，一般不需要进行调优
  redis:
    host: 101.200.178.253 # 地址
    port: 6379 # 端口
    database: 0 # 数据库索引
    password: pK8aeEsM260ryincBCAYmWcBSZNJB0jx
